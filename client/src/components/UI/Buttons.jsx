import React from 'react';

// Button Styles
const PrimaryButtonStyles = {};
const PrimaryIconButtonStyles = {};
const SecondaryButtonStyles = {};
const IconButtonStyles = {};

// Primary button with text
// Takes in argument via props for onClick event handler value,
// text value, and is active (disabled)
const PrimaryButton = (props) => {
  return (
    <button></button>
  );
};

// Primary button with text and icons
// Takes in argument onClick event handler value, text value,
// icon, and is active (disabled)
const PrimaryIconButton = (props) => {
  return (
    <button></button>
  );
};

// Secondary button with only text.
// Takes in argument onClick event handler value, text,
// and is active (disabled)
const SecondaryButton = (props) => {
  return (
    <button></button>
  );
};

// Icon button with only icon
// Takes in argument onClick event handler value, icon,
// and is active (disabled)
const IconButton = (props) => {
  return (
    <button></button>
  );
};

export {PrimaryButton, PrimaryIconButton, SecondaryButton, IconButton};

