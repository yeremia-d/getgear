import React from 'react';


/**
 * Pager functional component
 * @param {Object} props are the passed down properties
 * @return {JSX}
 * 2 properties are used: 'currentPage', 'totalPages'
 */
const Pager = (props) => {
  return (<div></div>);
};

export default Pager;
