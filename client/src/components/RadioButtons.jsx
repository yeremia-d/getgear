import React from 'react';

/**
 * Radio button function component
 * @param {Object} props contains the properties of the radio button
 *  text: text beside the radio button
 *  initialValue: initial value of the radiobutton (if cheched initially)
 *  isActive: true by default. if inactive, radio button cannot be clicked
 *  onClickHandler: event handler if the radiobutton is clicked.
 * @return {JSX}
 */
const radioButton = (props) => {
  return (<div></div>);
};

const radioGroup = (props) = {};

export {radioButton, radioGroup};
